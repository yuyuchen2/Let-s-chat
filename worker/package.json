{
  "name": "cloudflare-chat-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "wrangler dev --config ../wrangler.toml",
    "deploy": "wrangler deploy --config ../wrangler.toml"
  }
}
